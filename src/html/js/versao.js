/**
 * Versão exibida no rodapé do app (ver docs/especificacao.md secao 10).
 * Bump manual a cada release, sempre junto com VERSAO_APP em sw.js —
 * assim o rodapé só muda de número quando o service worker também tem
 * uma versão nova em cache pra servir.
 */
export const VERSAO_APP = "12";
